4. Copy the dependency file first to leverage Docker layer caching
COPY requirements.txt .

# 5. Install the required dependencies
RUN pip install --no-cache-dir -r requirements.txt

# 6. Copy the rest of the application source code into the container
COPY . .
 7. Inform Docker that the container listens on the specified network port
EXPOSE 8000

# 8. Define the default command to execute when the container starts
CMD [python, main.py]
